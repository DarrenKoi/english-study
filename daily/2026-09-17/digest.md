# 2026-09-17 — 오늘의 표현

- **the critical path** — 늦어지면 전체 일정이 그만큼 밀리는 작업 경로. *The letter sequence isn't the critical path to letter 18. Your site gates are.*
- **sitting in someone's queue** — 일이 막힌 원인이 작업량이 아니라 남의 승인 대기라고 짚을 때. ≈ pending approval.
- **X pays rent** — 번거롭던 규칙이 실제로 값을 했다. 표준형은 `earn its keep`.
- **one fat-fingered command away** — 오타 한 번이면 파괴적 동작이 실행되는 거리. `one X away` 틀로 앞말만 갈아 끼운다.
- **take its own word for it** — 검증 없이 자기 보고를 사실로 받는다. *The ledger was the one piece of state still taking its own word for it.*
- **scaffolding you then throw away** — 결과물을 만들고 나면 걷어 내는 비계. ↔ the lasting deliverable.
- **a ratchet rather than a treadmill** — 한 번 얻은 개선이 되돌아가지 않는 구조. ↔ running in place.

### 오늘의 정독
병렬 탭 제안에 대한 반박 단락 — `Your site gates are,` 로 보어를 생략한 대구를 세우고 `no number of pi tabs moves it`, `the schedule is set by A, not by B` 로 진짜 병목을 옮겨 놓는다. workbench 격리(`physically separated, not separated by care`)와 회귀 코퍼스(`without a single real byte leaving the FAB`) 단락은 `reading.md` 에 함께 있다.

### 오늘의 코칭
- 영어 다듬기: `make refresh mode as default` → `make refresh the default mode` (make + 목적어 + 명사 보어에는 `as` 를 넣지 않는다) / `let pi agent spawns` → `let the pi agent spawn` (let 뒤는 동사원형) / `if I face with the new conditions` → `when we run into new conditions` (능동 `face` 는 with 없이 목적어를 받는다).
- 오늘 `[user]` 메시지가 모두 영어라 한글→영어 카드는 없다. 영어 다듬기 14장은 `coaching.md`.

> 처리 항목 9개 / 미뤄진 항목 602개

# 2026-09-15 — 오늘의 표현

- **fall out for free** — 구조를 이렇게 잡으면 다른 기능이 저절로 딸려 나온다. *Two passes share the hash, so dedup and change detection fall out for free.*
- **One thing to watch:** — 보고 끝에 주의점 하나를 붙이는 문두. `One assumption to flag:` 도 같은 틀.
- **sufficient until proven otherwise** — 지금은 충분하고 더 복잡한 대안은 필요가 증명될 때 하자. ↔ already shown to be insufficient.
- **"Deprecated" and "vulnerable" are different things.** — 두 개념을 섞어 걱정하는 상대에게 별개임을 한 문장으로 못 박는다.
- **at the schema level, not by convention** — 규칙을 약속이 아니라 구조로 강제하라는 요구. ↔ on the honour system.
- **a courtesy for the reader, not the ground truth** — 참고용일 뿐 판단 근거가 아니라고 선 긋는 말.
- **X is off until Y finishes** — 형용사 `off` 하나로 "잠시 꺼 둔다"를 만들고 `until` 로 재개 조건을 건다.

### 오늘의 정독
아키텍처 critique 의 식별자·단위·계보 단락 — `X is Y` 정의문 여섯 개를 쌓고 `That is enough.` 세 단어로 끊은 뒤 `over-engineering at this stage` 로 판정하는 리듬. vLLM 준비 타임아웃 설명(`can't tell A from B`)과 critique 수정본(`changes A into B` 에 규칙 문장을 통째로 넣기)과 함께 `reading.md`.

### 오늘의 코칭
- 한글→영어: "qwen3.8-27b 준비 대기중 but it takes longer than 900s" → `While waiting for qwen3.8-27b to become ready, the launcher hits the 900s limit.` (한글 명사구는 절로 풀고 주어를 세운다)
- 영어 다듬기: `the info they filled in interfere each other` → `what they write interferes with each other` (`interfere` 는 자동사, `info` 는 단수) / `copy the folder into different name` → `copy the folder under a different name` / `it continues to make office branch` → `it keeps creating an office branch`.
- 전체 8장은 `coaching.md`. 도구가 생성해 넘긴 critique 요청문은 제외했다.

> 처리 항목 13개 / 미뤄진 항목 630개

# 2026-06-24 — 오늘의 표현

1. **That's the smoking gun** — 바로 그게 결정적 증거다(범인이다). 디버깅·회의 구어.
2. **degrade silently, never crash the loop** — 죽지 않고 조용히 기능만 낮춰 견디다. 안정성 설계 격식.
3. **a request ceiling, not a guarantee** — 요청 상한일 뿐 보장은 아니다. 설정값을 정확히 규정할 때.
4. **diminishing returns** — 수확 체감, 더 넣어도 이득이 줄어드는 구간. 분석 격식.
5. **in lockstep with** — 한 치 어긋남 없이 발맞춰 같이 움직이는. ↔ drift apart.
6. **gathering ≠ using** — 모으는 것과 (실제로) 쓰는 것은 다르다. 게이트가 끼었을 때.
7. **Right call given the data** — 데이터를 보면 옳은 결정이다. 근거 있는 동의.

### 오늘의 정독
빈 캐시를 두고 "팩토리는 로드됐는데 fetch 가 0을 반환했다"를 시점(import time / alarm time)으로
갈라 짚는 진단 단락 — `produces exactly your symptom`, `replace-if-non-empty` 같은 단정 어휘를
봅니다. (reading.md 단락 1)

### 오늘의 코칭
- 영어 다듬기: "when align fail happens" → **"When an alignment failure occurs"** (fail은 동사, 명사엔 관사 필요).
- 영어 다듬기: "removed the eqp_id out of the path" → **"removed the eqp_id from the path"** (remove A from B 연어).

> 처리 항목 10개 / 미뤄진 항목 0개

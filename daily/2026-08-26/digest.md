# 2026-08-26 — 오늘의 표현

1. **X was only a label.** — "1주 윈도"라고 써 있었지만 실제로는 최근 10회를 보고 있었다. 요청의 전제를 뒤집는 보고문 첫 줄.
2. **Reasonable, but invented.** — 타당하긴 한데 요청엔 없던 값. 품질 시비는 접고 출처 시비만 남기는 두 단어 판정.
3. **the data happens to be right, so the code was never asked to be** — 데이터의 우연한 모양에 기댄 불변식. 따옴표째 버그의 이름으로 쓴다.
4. **survives exactly until (someone edits a rule)** — "언젠가"가 아니라 바로 그 사건까지만 살아남는다.
5. **blocked at the payload, not the UI** — 막힌 층을 지목해 왜 그 옵션이 비싼지 설명한다.
6. **the fix is narrower than it might sound** — 진단은 길지만 고칠 곳은 한 줄. 상대를 안심시키며 여는 문장.
7. **One fork I don't want to guess on** — 추측으로 정하기엔 비싼 갈림길 하나를 사용자에게 넘기는 도입구.

### 오늘의 정독
단락 3 — Pool+PV 버그를 "지금은 맞지만 우연"으로 재정의하는 여섯 문장. `a property of A, not an invariant of B` 대비, 현재형 사실 뒤 가정법 `would happily claim` 전환, `lives … survives` 생존 동사로 시한부를 그리는 흐름. → `reading.md`

### 오늘의 코칭
- 한글→영어: "VG는 여러가지 의미로 사용되고 있어" → 문맥은 "표기"라 `"VG" shows up in several forms` — `several meanings` 로 직역하면 다른 질문이 된다. "판정이 데이터 순서에 의존하면 불변식이 아니라 우연" → `A verdict that depends on data order isn't an invariant; it's a coincidence.`
- 영어 다듬기: `enlarge the 1 week window` → `widen the 1-week window`(시간 범위는 widen/extend). `they look so compact` → `cramped`(compact 는 칭찬). `we may well see the tool lists` → `may well` 은 추측이라 `the tool list can follow from the model selection`. `HV-SEM experience the same path` → `goes through the same path`. → `coaching.md`

> 처리 항목 18개 / 미뤄진 항목 1052개

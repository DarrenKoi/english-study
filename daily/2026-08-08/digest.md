# 2026-08-08 — 오늘의 표현

- **coincide by construction** — 우연이 아니라 만든 방식 때문에 필연적으로 일치한다. 그 자리에서 하는 검증이 왜 무의미한지를 한 구로 끝낸다.
- **there's no room for X** — 값이 비어 있는 게 아니라 담을 자리 자체가 없다. 원인이 우리 코드가 아니라 상류 스키마에 있다는 신호.
- **whichever way that goes** — 그게 어느 쪽으로 결론 나든. 미결 항목 옆에 확정 항목을 붙일 때의 접속 장치.
- **aim the fix** — 고칠 능력은 있는데 표적이 없다. "못 하겠다"가 아니라 협조 요청으로 읽히게 만든다.
- **X doesn't stick** — 고쳐도 오래 못 가고 되돌아온다. 주어를 사람이 아니라 조치로 두는 게 핵심.
- **survive X** — 잘 처리한다가 아니라 터지지 않는다. 기대치를 일부러 낮춰 테스트가 보증하지 *않는* 것까지 전달한다.
- **can spare it** — 그만큼 내줄 여유가 있다. 같은 기능을 한 곳에만 켜는 이유를 설명할 때 딱 맞는다.

### 오늘의 정독

`activitySparkline.ts` 의 문서 주석 — 왜 이 모듈이 `echarts` 를 import 하지 않는지를 콜론과 가정법으로 논증합니다. `Keeping it out is what makes X testable` 의 what 분열문이 "여러 이유 중 이것이 결정적"이라는 강조를 만드는 자리입니다. → [reading.md](reading.md)

### 오늘의 코칭

- 한글→영어: "대문자여서 검색이 안되는거였어" → 에러가 아니라 빈 결과였으므로 `didn't work` 가 아니라 `was coming back empty` 로 증상을 특정합니다.
- 한글→영어(고급): "합법적 empty로 위장됩니다" → 한국어 피동을 영어에서는 `It disguises itself as a legitimate empty answer` 로 능동 전환합니다.
- 영어 다듬기: `the foremost important` → `foremost` 는 그 자체가 최상급이라 `most` 와 겹칠 수 없습니다.
- 영어 다듬기: `very thanks` → `very` 는 명사를 못 꾸밉니다. `thanks a lot` / `many thanks`.

→ [coaching.md](coaching.md)

> 처리 항목 17개 / 미뤄진 항목 0개

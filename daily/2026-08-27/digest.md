# 2026-08-27 — 오늘의 표현

1. **conceded, and it's the finding that mattered** — 외부 리뷰의 지적을 받아들이면서 그게 핵심이었다고 등급까지 매기는 판정문.
2. **more build than it buys** — 안전장치를 만드는 비용이 막아 줄 위험보다 크다. 거절에 이유를 넣는 두운 압축형.
3. **read the constant, not the data flow** — 상수 선언만 보고 실제 실행 경로는 안 봤다는 반박. `A, not B` 대비.
4. **nothing ties them, so drift surfaces only as runtime 400s** — 두 값을 묶는 검증이 없으면 어긋남은 가장 늦은 곳에서만 드러난다.
5. **The accepted loss:** — 트레이드오프의 단점을 숨기지 않고 "알고 감수한다"로 닫는 docstring 결말.
6. **reads the diff cold** — 사전 맥락 없이 읽는다. 외부 리뷰어의 단점을 장점으로 뒤집는 부사 `cold`.
7. **Tuning must not require a deploy.** — 값을 env 로 빼는 이유를 다섯 단어 제약으로.

### 오늘의 정독
단락 1 — 리뷰 finding 하나를 "수용 → 강조 `did` 로 직접 검증 → 과거형 연쇄 피해 → `was rationalising` 자백 → `Fixed:` 명사구 → 대시 뒤 경계선"으로 닫는 여섯 문장. → `reading.md`

### 오늘의 코칭
- 영어 다듬기: `keep 2주 as default. and user can decide from …` → `keep 2주 as the default, and let users choose from …`(정관사·`choose from`). `create diagnose script … the flow of process` → `a diagnostic script … the process flow`(형용사형·관사). `merge and push it once tests pass` 는 오류 없음 — 한 단계 위로 `once the suite is green`. → `coaching.md`

> 처리 항목 8개 / 미뤄진 항목 1015개

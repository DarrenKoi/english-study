# 2026-09-12 — 오늘의 표현

- **quietly diverge** — 오류 없이 결과만 갈라진다. "테스트 없는 경로가 바로 그 자리"라며 테스트를 요구할 때 쓴다.
- **a failure mode X deletes at the type level** — 조심할 문제가 아니라 타입으로 아예 없앨 문제라는 리팩터링 논거.
- **compares like with like** — 조건이 같은 것끼리 비교한다. `apples to oranges` 의 반대편.
- **leave it as is** — 리뷰 지적을 받고도 현 상태를 권할 때. `My recommendation is to …` 로 결론을 앞세운다.
- **the other way round / That way nobody gets locked out.** — 순서를 뒤집으면 어떻게 되는지, 이 순서면 무엇을 막는지 짝으로 설명한다.
- **Without this, every other improvement is guesswork.** — 우선순위를 주장하는 한 줄. `guesswork` 는 불가산.
- **That's expected, not a bug.** — 이상해 보이는 동작을 미리 설계대로라고 박아 둔다.

### 오늘의 정독
TTTM 리뷰 뒤 "그대로 두자"고 권하는 단락. 결론 먼저, 가정의 `would`, 앞 절 전체를 받는 `which is what`, 강조의 `do join` 을 본다. → `reading.md` 단락 2

### 오늘의 코칭
- 한글→영어: "추가: 반출 시 담당 MI팀 승인 필요." → `Exporting data requires approval from the responsible MI team.` 메모체 명사 나열은 동명사 주어로 문장을 세운다.
- 한글→영어: "~할 만큼만 떨어져 있습니다" → `just far enough apart that …`. "만"의 뉘앙스는 `just` 가 맡는다.
- 영어 다듬기: `write down what do I need to learn` → `what I need to learn`. 문장 속 의문문은 도치하지 않는다.
- 영어 다듬기: `selecting tools seem` → `seems`. 동명사 주어는 단수다.
→ `coaching.md`

> 처리 항목 36개 / 미뤄진 항목 0개

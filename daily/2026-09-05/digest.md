# 2026-09-05 — 오늘의 표현

- **ordered by payoff per effort** — 선택지를 늘어놓기 전에 정렬 기준을 한 구로 밝힌다.
  `Here are the levers, ordered by payoff per effort.`
- **a hint, not a cap** — 설정값이 권고인지 상한인지를 먼저 못 박는 대조 틀. ↔ a hard cap.
- **and this is the big one** — 목록 항목 중 하나에 무게를 몰아주는 삽입구.
- **Nothing else moves.** — 변경 목록을 닫으며 "나머지는 전부 그대로"를 세 단어로.
- **measured, not guessed** — 결정 근거가 실측임을 강조한다. ↔ a ballpark figure.
- **Anything that adds X is out.** — 제약 하나로 부류 전체를 탈락시키는 구어체.
- **they never abstain** — "모르겠다"를 못 내는 모델의 위험. ↔ fail closed.

### 오늘의 정독
[단락 1](reading.md) — `That settles it.` 로 결론을 먼저 던진 뒤 근거를 대는 순서. `would put` 가정법과 관계사 생략 `the process it kills`, 그리고 any/every 대비로 배제와 잔여를 짝짓는 마무리를 본다.

### 오늘의 코칭
- 한글→영어: "VLM이기 보다는 소형 LLM을 도입해서 판단을 맡기는 건 어떨까?" → `Rather than a VLM, what about bringing in a small LLM that also has vision and letting it make the decisions?`
- 한글→영어(번역): "얻는 건 없고 잃는 건 재현성입니다" → `gains you nothing and costs you reproducibility` 로 동사 둘의 병렬.
- 영어 다듬기: `enhance the latency` 는 지연을 키운다는 뜻. `reduce/cut the latency`, 그리고 선택지를 원할 땐 `What are my options for ...`.
- 영어 다듬기: `what is the pros and cons ... in the both GPU` → `What are the trade-offs of running qwen across both GPUs alongside the other two models?`

전체는 [coaching.md](coaching.md).

> 처리 항목 31개 / 미뤄진 항목 900개

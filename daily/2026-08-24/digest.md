# 2026-08-24 — 오늘의 표현

1. **a fresh heartbeat over missing data** — 데이터는 비었는데 최신 시각만 찍혀 있는 상태. 모니터링에서 가장 위험한 실패를 은유 하나로 짚는다.
2. **just the request people noticed** — 신고된 증상은 원인이 아니라 눈에 띈 표본일 뿐. 사용자를 부정하지 않고 초점만 옮기는 말.
3. **has to argue for itself** — 다음에 이 목록에 뭘 넣으려는 사람은 스스로 근거를 대야 한다. 주석 한 줄로 입증 책임을 넘긴다.
4. **the objections that landed** — 실제로 먹힌 지적들. 권투 비유라 "말해졌다"와 "맞았다"를 가른다.
5. **the ceiling, not the target** — 상한이지 채워야 할 목표가 아니다. 예산·재시도 횟수가 목표치로 변질되는 걸 막는 한 구절.
6. **blow through (a budget)** — 한도를 단숨에 써 버리다. `exceed` 와 달리 속도와 무신경까지 담긴다.
7. **stale but still green** — 낡았는데 여전히 통과하는 테스트. 초록불이 유효함의 증거가 아니라는 지적.

### 오늘의 정독
`live_alarm` 설계서의 ZSET 절 — "왜 마지막 응답만 캐시하지 않았는가"를 여섯 문장으로 답한다. 마지막 줄 `Idempotence is what allows ...` 의 what-분열문이 단락 전체의 결론을 한 단어에 얹는 장치. → `reading.md`

### 오늘의 코칭
- 한글→영어: "클릭까지 가도록 해줘" → `Take it all the way through the click.`(`up to the click` 은 "직전까지"라 정반대). "누가 이미 점유 중일 때는 건너뛰는 거지?" → `So it skips when someone already has the tool, right?`
- 영어 다듬기: `monitorings` 는 불가산이라 복수형이 없고(→ `monitoring output`), `sort of things that I can see if ...` 는 `so that I can see whether ...` 가 맞다. `switch in build mode` → `switch to build mode`, `it tend` → `they tend`. → `coaching.md`

> 처리 항목 14개 / 미뤄진 항목 1308개

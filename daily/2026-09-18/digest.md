# 2026-09-18 — 오늘의 표현

- **map the blast radius** — 고치기 전에 변경이 어디까지 번지는지 조사하다. *Still mapping the blast radius.* ≈ scope out the impact.
- **quietly bring back** — 한쪽만 고치면 복사 과정에서 옛 버그가 소리 없이 되살아난다. *A later re-copy would quietly bring back the ZoneInfo import.*
- **let (something) through** — 검사나 정책이 막아야 할 것을 통과시키다. 목적어는 `let` 과 `through` 사이에 넣는다. ↔ block, refuse.
- **I read X as Y** — 모호한 지시를 어떻게 해석했는지 먼저 밝히는 틀. ≈ I took X to mean Y.
- **orphaned, not migrated** — 옛 키를 옮기지 않고 버려 둔다는 결정을 `A, not B` 로 못 박는다.
- **hide behind a fresher sibling** — 나쁜 상태 하나가 집계 뒤에 가려지는 현상. 짝으로 `worst-of merge` 를 함께 익힌다.
- **pick up the fix** — 따로 할 일 없이 다음 갱신 때 수정분을 받아 간다. *The office picks up the fix when the engineer copies the repo over.*

### 오늘의 정독
"왜 다른 저장소를 건드렸느냐"에 답하는 단락 — `If only this repo changed, a later re-copy … would quietly bring back …, and Windows would break again` 의 가정법 과거로 결과의 연쇄를 그리고, `If you'd rather not …, I can …` 으로 되돌릴 길을 연 뒤 `The catch is that …` 으로 대가를 붙인다. 현재형 설명 속에 `it would have been closed` 가 끼어드는 단락과 설계 문서의 현재형 단락은 `reading.md` 에 함께 있다.

### 오늘의 코칭
- 영어 다듬기: `why skewnono_v3_nuxt is involved` → `why is skewnono_v3_nuxt involved` (직접 의문문은 be 동사가 주어 앞) / `except the files are made` → `unless the files …` (except 뒤에는 절이 바로 못 온다) / `skip to search the tool` → `skip searching for the tool` (`skip to X` 는 "X 로 건너가라"라 뜻이 뒤집힌다).
- 오늘 `[user]` 메시지가 모두 영어라 한글→영어 카드는 없다. 영어 다듬기 10장은 `coaching.md`.

> 처리 항목 10개 / 미뤄진 항목 593개
